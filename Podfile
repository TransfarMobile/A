# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

source 'https://TransfarMobile@github.com/TransfarMobile/TFSpecs.git'
source 'https://github.com/CocoaPods/Specs.git'

target 'A' do
  pod "CTMediator"
  pod "HandyFrame"
  # use_frameworks!
 
end
